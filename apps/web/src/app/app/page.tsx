import { currentActor } from "@/lib/server-api";

export default async function AppPage(){const actor=await currentActor();return <main className="py-16"><p className="text-sm text-emerald-400">Human-supervised engineering control plane</p><h1 className="mt-3 text-4xl font-semibold">Welcome, {actor?.user.display_name}</h1><div className="mt-10 rounded-xl border bg-slate-950/50 p-6"><p className="text-sm text-slate-500">Current organization</p><p className="mt-2 text-xl font-medium">{actor?.organization.name}</p><p className="mt-1 text-sm text-slate-400">Role: {actor?.membership.role}</p></div><p className="mt-8 max-w-xl text-slate-400">Create durable engineering tasks, inspect immutable repository snapshots, and make explicit approval decisions. AI analysis and repository modifications are not implemented.</p></main>}
