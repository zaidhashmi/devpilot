import { repositories } from "@/lib/server-api";

export default async function RepositoriesPage(){const items=await repositories();return <main className="py-16"><p className="text-sm text-emerald-400">Repository catalog</p><h1 className="mt-2 text-3xl font-semibold">Repositories</h1><p className="mt-3 max-w-2xl text-slate-400">Repositories authorized through the connected GitHub App. This catalog contains metadata only; source code is not stored or executed.</p>{items.length===0?<div className="mt-8 rounded-xl border border-dashed p-8 text-slate-400">No repositories are currently available.</div>:<div className="mt-8 overflow-hidden rounded-xl border"><table className="w-full text-left text-sm"><thead className="bg-slate-950/70 text-slate-400"><tr><th className="px-5 py-3">Repository</th><th className="px-5 py-3">Visibility</th><th className="px-5 py-3">Default branch</th><th className="px-5 py-3">Status</th></tr></thead><tbody>{items.map(repo=><tr className="border-t" key={repo.id}><td className="px-5 py-4 font-medium">{repo.full_name}</td><td className="px-5 py-4">{repo.private?"Private":"Public"}</td><td className="px-5 py-4 text-slate-400">{repo.default_branch}</td><td className="px-5 py-4">{repo.archived?"Archived":repo.disabled?"Disabled":"Available"}</td></tr>)}</tbody></table></div>}</main>}
