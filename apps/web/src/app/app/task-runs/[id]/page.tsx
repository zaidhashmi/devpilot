import{notFound}from"next/navigation";import{taskRun}from"@/lib/server-api";import{TaskRunStatus}from"@/components/task-run-status";
export default async function RunPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const run=await taskRun(id);if(!run)notFound();return <main className="py-16"><p className="text-sm text-emerald-400">Task run {run.run_number}</p><h1 className="mt-2 text-3xl font-semibold">Durable repository workflow</h1><p className="mt-3 text-sm text-slate-400">{run.repository_full_name} · {run.requested_ref}</p><p className="mt-2 break-all font-mono text-xs text-slate-500">Immutable commit: {run.resolved_commit_sha}</p><TaskRunStatus initial={run}/></main>}
